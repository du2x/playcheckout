/**
 * Pure round simulation. Phase 1 ships a placeholder; Phase 2 builds the full
 * 20 Hz state machine here (inputs + time in, events out).
 */

/** Locked in prd §11: authoritative server, 20 Hz tick. */
export const TICK_HZ = 20
